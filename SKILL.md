---
name: healthcare-site-playbook
description: 大健康/养老/医疗行业企业展示型官网从 0 到交付上线的完整工作流。当用户要建企业官网、机构官网、品牌展示站，尤其提到"对标模仿""复用开源模板""新手一步一步"时使用。强制六阶段确认门流程：需求澄清→对标拆解→开源选型→搭建→三端验收→部署上线，每阶段必须先产出交付物并经用户确认才进入下一阶段。
---

# 大健康官网搭建工作流（六阶段确认门）

## 铁律（先于一切）

1. **不许跳过需求直接搭**。必须从阶段 0 开始，把 `references/01-需求澄清清单.md` 里的关键问题问完、用户确认后，才能进入下一阶段。
2. **每阶段结束必须停下**：产出本阶段交付物 → 简述结论 → 明确问用户"确认进入下一阶段吗？"。用户说继续才继续。
3. **只拆不建 / 只建不部署**：用户没要求部署前，搭建止步于本地构建验证；部署必须用户明确提出。
4. **合规红线**（医疗养老行业）：不绝对化用语（禁"最/百分百"）、不贬低同行、不夸大疗效、不用"免费"灰区词、数据要有出处。文案产出前先讲合规判断。
5. 全程中文沟通，步骤写到新手可复制执行的程度（含完整命令、路径、点击位置）。

## 阶段 0：需求澄清

读 `references/01-需求澄清清单.md`，按清单逐项向用户提问（可多轮，用选项题降低回答成本）。必拿答案：品牌名、栏目结构、主色调、目标受众、素材情况、合规约束、域名/服务器现状、上线时间预期。
**交付物**：需求确认表（markdown 表格，写入项目目录）。
**确认门**：用户回复确认需求表无误 → 进阶段 1。

## 阶段 1：对标拆解

读 `references/02-对标拆解方法.md`。和用户一起定 1~3 个同行业对标官网，用无头浏览器真实渲染逐屏截图 + 抓页面结构，拆出：导航结构、首页板块流、每板块效果、视觉风格（主色/字体/图片风格）、转化入口、页脚合规要素。
**交付物**：对标拆解笔记.md + 截图存档目录。
**确认门**：用户认可拆解结论 → 进阶段 2。

## 阶段 2：开源选型

读 `references/03-开源选型规则.md`。按 6 条一票否决门槛 + 30 分评分表在 GitHub 筛候选（关键词包在规则里），评估 2~3 个，推荐 1 个并说明共同缺口（如轮播库、表单后端）。
**交付物**：候选项目评估报告.md。
**确认门**：用户拍板底座 → 进阶段 3。

## 阶段 3：搭建

读 `references/04-搭建实施手册.md`。流程：克隆底座（国内走镜像代理下 ZIP）→ 精简多余模块 → 全局换主色 → 换品牌资产 → 按对标结构写首页板块组件 → 二级页 → `npm run build` 通过。素材缺口用【待补充】占位，**严禁编造企业数据**。
**交付物**：本地 `npm run preview` 可访问的完整站点。
**确认门**：构建 0 错误 → 进阶段 4。

## 阶段 4：三端验收

读 `references/05-验收清单.md`。用 playwright-core 直驱 Chromium 对全部页面做 1440/768/390 三宽度截图，逐张目检：板块完整、图片不裂、无横向滚动条、移动端可点。
**交付物**：三端验收截图包 + 问题清单（如有问题修完复验）。
**确认门**：用户看过截图确认 → 进阶段 5（或按意见迭代回阶段 3）。

## 阶段 5：部署上线

读 `references/06-部署上线手册.md`。默认路线：阿里云轻量服务器 + Nginx + IP:8080 预览（阶段 A，当天可成）；域名 + ICP 备案 + HTTPS（阶段 B，2~4 周，需公司主体）。全程给用户可复制的命令和点击路径。
**交付物**：公网可访问地址 + 部署记录（服务器信息脱敏）。
**确认门**：用户验收通过 → 项目交付，输出后续更新流程说明。

## 常见踩坑速查（详见证 04/06）

- 本机构建必须 `NODE_OPTIONS="" npm run build`（系统钩子拦截 Astro 清缓存）
- GitHub 克隆卡死 → 用镜像代理下载 ZIP：`https://ghfast.top/<原始zip链接>`
- npm 装依赖走 `--registry=https://registry.npmmirror.com`
- 大陆服务器未备案：用 IP+8080 预览，别碰 80/443 对外
- 域名必须以备案主体（公司）名义购买实名
